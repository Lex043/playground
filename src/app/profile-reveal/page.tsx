import React from "react";
import ProfileRevealAnimation from "@/components/profile-reveal-animation";

export default function Page() {
    return (
        <section>
            <ProfileRevealAnimation />
        </section>
    );
}
